{
  "version": 1,
  "servers": {
    "qwen_omarchy_control": {
      "enabled": true,
      "transport": {
        "type": "stdio",
        "command": "/home/neil/.local/share/qwen-omarchy-control/bin/desktop-mcp",
        "args": [],
        "env": {
          "PYTHONPATH": "/home/neil/.local/share/qwen-omarchy-control/src"
        },
        "cwd": "/home/neil/.local/share/qwen-omarchy-control"
      },
      "tools": {
        "get_active_window": { "enabled": true, "description": "That focused window." },
        "list_windows": { "enabled": true, "description": "All open windows with their class, title and workspace." },
        "list_workspaces": { "enabled": true, "description": "All workspaces, their monitor and window counts." },
        "get_monitors": { "enabled": true, "description": "Connected displays." },
        "switch_workspace": { "enabled": true, "description": "Switch to a numbered workspace (1-100)." },
        "focus_window": { "enabled": true, "description": "Focus a window by its class or title." },
        "launch_app": { "enabled": true, "description": "Launch an installed application; also 'terminal', 'browser', 'files', or the agent TUIs 'hermes'/'codex'/'opencode'." },
        "launch_agent": { "enabled": true, "description": "Open a visible agent TUI (hermes/codex/opencode) with the spoken prompt submitted, so the user can watch it work." },
        "set_volume": { "enabled": true, "description": "Set speaker volume to a percent (0-100)." },
        "volume_up": { "enabled": true, "description": "Raise speaker volume one step." },
        "volume_down": { "enabled": true, "description": "Lower speaker volume one step." },
        "mute_audio": { "enabled": true, "description": "Mute the computer's speakers." },
        "unmute_audio": { "enabled": true, "description": "Unmute the computer's speakers." },
        "get_audio_status": { "enabled": true, "description": "Current volume and mute state." },
        "get_system_status": { "enabled": true, "description": "OS, kernel, hostname, uptime, memory, time." },
        "read_window": { "enabled": true, "timeoutMs": 30000, "maxResultBytes": 65536, "description": "OCR a window and return its text - used to read back what an app/agent produced." },
        "read_screen": { "enabled": true, "timeoutMs": 30000, "maxResultBytes": 65536, "description": "OCR the whole focused monitor." },
        "pointer_move": { "enabled": true, "description": "Move the mouse pointer (absolute x,y, or a delta with relative=true)." },
        "mouse_click": { "enabled": true, "description": "Click the left/right/middle button at the pointer (optionally double)." },
        "mouse_scroll": { "enabled": true, "description": "Scroll a window (or the pointer's location) up/down/left/right by pages." },
        "move_active_window_to_workspace": { "enabled": true, "description": "Move the focused window to a numbered workspace." },
        "close_active_window": { "enabled": true, "description": "Close the focused application window (asks you to confirm first)." },
        "open_url": { "enabled": true, "description": "Open an http(s) URL in the default browser." },
        "type_text": { "enabled": true, "description": "Focus a window and type text (optionally press Enter to send). Used to hand prompts to the user's coding agent window ('opencode')." },
        "confirm_pending": { "enabled": true, "description": "Run the action that is waiting for confirmation." },
        "cancel_pending": { "enabled": true, "description": "Discard the action that is waiting for confirmation." }
      }
    }
  }
}