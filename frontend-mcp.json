{
  "version": 1,
  "servers": {
    "qwen_omarchy_control": {
      "enabled": true,
      "transport": {
        "type": "stdio",
        "command": "/home/neil/.local/share/qwen-omarchy-control/bin/desktop-mcp",
        "args": [],
        "env": {
          "PYTHONPATH": "/home/neil/.local/share/qwen-omarchy-control/src"
        },
        "cwd": "/home/neil/.local/share/qwen-omarchy-control"
      },
      "tools": {
        "get_active_window": {
          "enabled": true,
          "description": "That focused window."
        },
        "list_windows": {
          "enabled": true,
          "description": "All open windows with their class, title and workspace."
        },
        "list_workspaces": {
          "enabled": true,
          "description": "All workspaces, their monitor and window counts."
        },
        "get_monitors": {
          "enabled": true,
          "description": "Connected displays."
        },
        "switch_workspace": {
          "enabled": true,
          "description": "Switch to a numbered workspace (1-100)."
        },
        "focus_window": {
          "enabled": true,
          "description": "Focus a window by its class or title."
        },
        "launch_app": {
          "enabled": true,
          "description": "Launch an installed application; also 'terminal', 'browser', 'files', or the agent TUIs 'hermes'/'codex'/'opencode'."
        },
        "launch_agent": {
          "enabled": true,
          "description": "Open a visible agent TUI (hermes/codex/opencode) with the spoken prompt submitted, so the user can watch it work."
        },
        "set_volume": {
          "enabled": true,
          "description": "Set speaker volume to a percent (0-100)."
        },
        "volume_up": {
          "enabled": true,
          "description": "Raise speaker volume one step."
        },
        "volume_down": {
          "enabled": true,
          "description": "Lower speaker volume one step."
        },
        "mute_audio": {
          "enabled": true,
          "description": "Mute the computer's speakers."
        },
        "unmute_audio": {
          "enabled": true,
          "description": "Unmute the computer's speakers."
        },
        "get_audio_status": {
          "enabled": true,
          "description": "Current volume and mute state."
        },
        "get_system_status": {
          "enabled": true,
          "description": "OS, kernel, hostname, uptime, memory, time."
        },
        "read_window": {
          "enabled": true,
          "timeoutMs": 30000,
          "maxResultBytes": 65536,
          "description": "OCR a window and return its text - used to read back what an app/agent produced."
        },
        "read_screen": {
          "enabled": true,
          "timeoutMs": 30000,
          "maxResultBytes": 65536,
          "description": "OCR the whole focused monitor."
        },
        "find_element": {
          "enabled": true,
          "description": "Locate a UI element by name using the window's accessibility tree (role, label, position). Read-only; prefer over OCR when a control has a clear label."
        },
        "find_text": {
          "enabled": true,
          "description": "Find text on screen by OCR and return its screen x,y so you can click it. The coordinate half of read_screen: use it when a control has no accessibility label (a video thumbnail, a canvas, anything inside Chrome). Then pointer_move to x,y and mouse_click. Read-only."
        },
        "describe_actions": {
          "enabled": true,
          "description": "Answer 'what can I do here?' for a window: read its accessibility tree and name the 3-5 meaningful actions (open, save, send, delete, navigate), best first. Read-only, no click. Use when the user asks what they can do in the current window, or what is on screen, or to orient before clicking."
        },
        "describe_outcome": {
          "enabled": true,
          "description": "Answer \"did it work?\" about a GUI action: read the window and judge whether the goal is now satisfied; returns a `spoken` sentence to read aloud. Read-only, never clicks."
        },
        "browser_read": {
          "enabled": true,
          "description": "Read the current browser page as real DOM elements (role, name, clickable) instead of OCR. Exact where read_screen guesses. Read-only, no mouse movement. Use whenever the browser is the window in question; optional goal narrows it (e.g. 'the search box')."
        },
        "browser_click": {
          "enabled": true,
          "description": "Click a link/button on the current browser page by name, through the browser's own DOM. No OCR and no pointer movement - it runs in the background, so no takeover announcement is needed. Use this instead of click_element for anything inside Chrome. Verifies URL/title and reports verified honestly."
        },
        "browser_type": {
          "enabled": true,
          "description": "Type text into a field on the current browser page, found by name (or the only text field). No pointer movement. Use for search boxes and ordinary forms; NEVER for password, payment or authentication fields."
        },
        "browser_navigate": {
          "enabled": true,
          "description": "Navigate the browser's current tab to an http(s) URL. Prefer open_url for opening a new page; use this to change the page the agent is already working in."
        },
        "browser_search": {
          "enabled": true,
          "description": "Search the web and land on the results page in one step: navigates, types the query, submits, and verifies the results loaded. Prefer this over driving a search engine manually. Use for 'look up X', 'search for X', 'google X'."
        },
        "click_element": {
          "enabled": true,
          "description": "Find a UI element by name and click it via the accessibility tree. Moves the real mouse and takes focus for a moment - announce this to the user first. double=true opens an item."
        },
        "do_gui_task": {
          "enabled": true,
          "description": "Run a short, bounded, verified GUI task in one window when simple primitives are not enough (e.g. 'open the Downloads folder', 'fill the name field and submit'). Each step is chosen from options built from the window's live state, acted on, then re-read to confirm what changed. Literal text must be passed in `inputs`; never invented. Returns status done/blocked/needs_agent and never a false success. Use for a handful of steps, not a long march."
        },
        "do_sequence": {
          "enabled": true,
          "description": "Run several GUI steps in order in one request, stopping at the first step that cannot be verified and naming it. Returns per-step results and a `spoken` summary."
        },
        "macro_record": {
          "enabled": true,
          "description": "Record a desktop chore once so it can be replayed by name. action=start/stop/list/delete."
        },
        "macro_replay": {
          "enabled": true,
          "description": "Replay a saved macro by name: re-runs its recorded steps, re-targets live elements, verifies each step, and stops honestly at the failing step. Returns a `spoken` summary."
        },
        "pointer_move": {
          "enabled": true,
          "description": "Move the mouse pointer (absolute x,y, or a delta with relative=true)."
        },
        "mouse_click": {
          "enabled": true,
          "description": "Click the left/right/middle button at the pointer (optionally double)."
        },
        "mouse_scroll": {
          "enabled": true,
          "description": "Scroll a window (or the pointer's location) up/down/left/right by pages."
        },
        "move_active_window_to_workspace": {
          "enabled": true,
          "description": "Move the focused window to a numbered workspace."
        },
        "close_active_window": {
          "enabled": true,
          "description": "Close the focused application window (asks you to confirm first)."
        },
        "open_url": {
          "enabled": true,
          "description": "Open an http(s) URL in the default browser."
        },
        "type_text": {
          "enabled": true,
          "description": "Focus a window and type text (optionally press Enter to send). Used to hand prompts to the user's coding agent window ('opencode')."
        },
        "confirm_pending": {
          "enabled": true,
          "description": "Run the action that is waiting for confirmation."
        },
        "cancel_pending": {
          "enabled": true,
          "description": "Discard the action that is waiting for confirmation."
        },
        "watch_start": {
          "enabled": true,
          "description": "Watch a window and report when a condition becomes true. Read-only. Returns a job_id to poll with watch_check."
        },
        "watch_check": {
          "enabled": true,
          "description": "Check a watch job: status running/met/timeout/stopped plus a `spoken` sentence when done."
        },
        "watch_stop": {
          "enabled": true,
          "description": "Stop a watch job."
        }
      }
    }
  }
}
