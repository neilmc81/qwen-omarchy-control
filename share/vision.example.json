{
  "_comment": "Copy to ~/.config/qwen-omarchy-control/vision.json (0600). Controls precise element targeting for find_element/describe_actions/click_element. These tools are on by default and self-degrade to a clear 'unavailable' message (missing cua-driver, key, or accessibility tree), so no config file is needed to use them. Set \"enabled\": false only to remove the capability.",
  "enabled": true,
  "announceTakeover": true,
  "verify": true,
  "verifyDelayMs": 600,
  "retries": 0,
  "audit": true,
  "jevOutcome": false,
  "jevOutcomeThreshold": 0.7,
  "minConfidence": 0.6,
  "maxCandidates": 40,
  "timeoutMs": 12000,
  "driverTimeoutMs": 20000,
  "model": "typesafe/jev-1.13",
  "endpoint": "https://openrouter.ai/api/alpha/decisions",
  "apiKeyEnv": "OPENROUTER_API_KEY",
  "driver": "cua-driver",
  "enableWayland": true
}
